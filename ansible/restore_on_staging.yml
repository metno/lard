---
- name: Restore dump on staging
  hosts: staging
  remote_user: ubuntu
  gather_facts: true
  roles:
    - role: recovery
      vars:
        recovery_mount_point: '{{ ostack_mount_point }}'
        recovery_pg_version: 17
